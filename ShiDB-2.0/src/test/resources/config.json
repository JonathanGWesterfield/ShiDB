{
  "buffer_mgr_selection_strategy": "NAIVE",
  "_selection_strategies": [
    "NAIVE",
    "FIFO",
    "LRU",
    "CLOCK"
  ],
  "buffer_mgr_pin_max_wait_time_milliseconds": 2000,
  "buffer_mgr_pin_poll_step_milliseconds": 100,
  "size_of_buffer_pool": 3,
  "db_file_block_size": 400,
  "recovery_manager_recovery_strategy": "UNDO_ONLY",
  "_recovery_manager_strategies": [
    "UNDO_ONLY",
    "UNDO_REDO"
  ],
  "concurrency_mgr_acquire_lock_max_wait_time_milliseconds": 3000,
  "_concurrency_mgr_acquire_lock_max_wait_time_comment": "Ensure that this value is larger than the Buffer Mgr max wait time. If the buffer mgr is deadlocking on pinning a buffer, it can cause the ConcurrencyMgr on top to also timeout. If the Concurrency Mgr times out first, it will mask the real cause of the timeout",
  "concurrency_mgr_acquire_lock_poll_step_milliseconds": 100,
  "concurrency_mgr_poll_step_waiting_for_transaction_drain_q_checkpoint_milliseconds": 100,
  "concurrency_mgr_checkpoint_every_n_transactions": 10,
  "concurrency_mgr_use_NQ_checkpoints": true,
  "_checkpoint_every_n_transactions_comment_1": "The default recover strategy (undo-only) makes checkpointing expensive. Undo-redo makes it free",
  "_checkpoint_every_n_transactions_comment_2": "If using the undo-only strategy, need to implement checkpointing every n seconds instead"
}